({	
  
    //this action function will not contain any business logic
    //it only contains catching actions
	nameAction : function(component, event,helper) {
		
        helper.nameHandler(component,event,name);
        
	}
})